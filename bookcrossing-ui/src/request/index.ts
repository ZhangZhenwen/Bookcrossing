import request, {get, post, put, del} from "@/request/http";
import url from "@/request/url"

/**
 * @author zhenwen
 * @date 2021/9/9
 */

export default {
    request,
    get,
    post,
    put,
    del,

    url,
}